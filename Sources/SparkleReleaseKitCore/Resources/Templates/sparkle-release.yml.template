name: Sparkle release readiness

on:
  pull_request:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sparkle-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: LeonTOfficial/SparkleReleaseKit/.github/workflows/reusable-validate.yml@6dc0f343831f3e05d16e57d4c433c262386e1943 # v0.1.1
    with:
      container: {{CONTAINER_YAML}}
      scheme: {{SCHEME_YAML}}
      configuration: {{CONFIGURATION_YAML}}
      config-path: sparklekit.json
